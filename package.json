{
    "name": "volleyballbot-vercel",
    "version": "1.0.0",
    "scripts": {
        "start": "vercel dev",
        "build": "vercel build",
        "deploy": "vercel deploy",
        "test": "python -m pytest tests/",
        "test-cov": "python -m pytest tests/ --cov"
    },
    "dependencies": {
        "@vercel/node": "^24.0.10",
        "telegraf": "^4.11.1",
        "better-sqlite3": "^8.6.0",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "vercel": "^24.1.4",
        "pytest": "^8.0.0",
        "pytest-cov": "^3.0.0",
        "mypy": "^0.961"
    }
}
